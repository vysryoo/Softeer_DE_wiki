# [2026-07-21] 회고

---

## Review (리뷰)
 - NYC TLC Trip Record 데이터를 Spark DataFrame으로 적재하고, 결측치 처리와 시간 필드 표준화, 음수 거리·소요시간 등 비정상 레코드를 필터링하는 데이터 정제 파이프라인을 구현했다.
 - 정제된 데이터를 기반으로 평균 운행 시간과 평균 운행 거리를 계산하고, 시간대별 운행 건수를 집계해 피크 시간대를 도출했다.
 - 외부 날씨 데이터셋을 결합하여 기온·강수량과 택시 수요 간의 상관관계를 통계적 방법으로 분석했다.
 - 계산된 지표와 분석 결과를 CSV/Parquet 파일로 저장하고, Jupyter Notebook에서 시간대별 운행 분포와 날씨-수요 상관관계를 시각화했다.

## 잘한 점
 - 결측치 처리, 시간 필드 표준화, 비정상 레코드 필터링까지 포함한 데이터 정제 파이프라인을 처음부터 끝까지 구현했다.
 - 외부 날씨 데이터를 결합해 통계적 방법으로 상관관계를 검증하고, 결과를 Jupyter Notebook으로 시각화까지 완결했다.

## 아쉬운 점
 - 상관관계 분석에 사용한 통계적 방법(검정 방식, 유의수준 등)의 근거를 구체적으로 정리하지 못했다.
 - 단일 시기 데이터만으로 분석해 계절성 등 다른 기간에도 일반화되는지는 확인하지 못했다.


---